import { Component } from 'react';
import './App.scss';
import TraderDashboard from './TraderDashboard/TraderDashboard';

export default class App extends Component {
  render() {
    return (
      <div>
        <TraderDashboard />
      </div>
    );
  }
}
